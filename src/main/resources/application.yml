spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/coffee
    username: root
    password: mmin0307
  jpa:
    database: mysql
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
  upload:
    photo:
      images: src/main/resources/static/images
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

jwt:
  secret: your-secret-key-here-make-it-long-and-secure-for-production-use-in-real-environment
  expiration: 86400000
  issuer: coffee-analyze

frontend:
  auto-start: true
  dev-server-url: http://localhost:5173